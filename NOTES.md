# TODO

## Swimhistory_DB

Sorting mechanism for the table
Table joins for IDs, so school_id becomes school name
Data Validation for the form
DateTime filter so it is always readable
"Post successful/error" notify (toast in general)
A storage element for last posted query, like a history
Database metadata, such total records, tables, dataflow

## Site Admin

User authentication
Seperate tab but same styles for Site admin, users, posts, articles, images
Site traffic/analytics in same dashboard
User upload space

## Actual website

Full Search view. Users can search any keyword and get relevant results in block format.
i.e. "Karen Muir" will return a "Swimmers" type result (big block square that leads to a profile), "pool" type results in a similar fashion, then a searchabe Virtual table for actual records, with her's brought to the top/sorted.

User suggestion integration -- users can suggest if something is incorrect.

Image Database view -- OCR?
